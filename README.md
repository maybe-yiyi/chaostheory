# Chaos Theory Simulator

hopefully eventually a better version of my other project

## Building

To build the project:

`$ cmake -S. -Bbuild && cmake --build build`

To run the project,

`$ ./build/bin/main`
